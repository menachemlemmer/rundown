<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="shortcut icon" type="image/png" href="/images/favicon.png?" />
    <script src="https://cdn.jsdelivr.net/npm/html-duration-picker@latest/dist/html-duration-picker.min.js"></script>
</body>
  </head>
  <body>
    <%- include('../partials/_navbar') %>

    <div class="body-container">
      <div class="glass form">
        <h1>Edit <%= run.title %></h1>
        <form action="/runs/<%= run._id %>?_method=PUT" method="POST" class="form">
          <div>
            <label for="title">Title</label>
            <input type="text" name="title" id="title" value="<%= run.title %>" required />
          </div>
          <div>
            <label for="distance">Distance in miles</label>
            <input type="number" name="distance" id="distance" step="0.01" value="<%= run.distance %>" required />
          </div>
          <div>
          <label for="time">Duration</label>
            <input
              type="text"
              name="time"
              id="time"
              class="html-duration-picker"
              value="<%= run.time %>"
              required
            />
          </div>
          <div>
            <label for="date">Date</label>
            <input type="datetime-local" name="date" id="date" value="<%= date %>" required />
          </div>
        <button type="submit">Submit</button>
        </form>
      </div>
    </div>
  </body>
</html>