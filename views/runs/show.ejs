<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= run.title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="body-container">
      <div class="glass show">
        <h1><%= run.title %></h1>
        <p>Distance: <%= run.distance %> mi.</p>
        <p>Time: <%= run.time %></p>
        <p>Speed: <%= run.speed %> miles per minute</p>
        <p>Date: <%= run.date %></p>
        <form
          action="/runs/<%= run._id %>?_method=DELETE"
          method="POST"
          class="show"
        >
          <a href="/runs/<%= run._id %>/edit">Edit Run</a>
          <button type="submit">Delete run</button>
        </form>
      </div>
    </div>
  </body>
</html>
